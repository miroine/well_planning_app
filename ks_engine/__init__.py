"""Well Planning App physics engine (pure NumPy, no Streamlit).

Internal conventions: field units (ft, lbf, ft-lbf, psi, ppg, gpm, in) and USD,
except trajectory geometry which is unit-agnostic (the UI feeds metres).
"""
